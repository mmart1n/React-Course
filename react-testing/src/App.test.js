import { render, screen } from "@testing-library/react";
import App from "./App";

// test("renders learn react link", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/learn react/i); // get element ref by the text rendered inside it
//   expect(linkElement).toBeInTheDocument();
// });
